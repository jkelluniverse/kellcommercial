# Railway build configuration — single-service deployment.
# Builds the React frontend into static files, installs Python deps inside a
# virtualenv (Nix's Python is immutable, so pip must go in a venv), and runs
# FastAPI which serves both /api/* and the React app from one process.

[phases.setup]
nixPkgs = ["python311", "nodejs_20", "gcc"]

[phases.install]
cmds = [
  "cd frontend && npm install --legacy-peer-deps",
  "python3 -m venv /opt/venv",
  "/opt/venv/bin/pip install --upgrade pip",
  "/opt/venv/bin/pip install -r backend/requirements.txt",
]

[phases.build]
cmds = [
  "cd frontend && CI=false npm run build",
]

[start]
cmd = "cd backend && /opt/venv/bin/uvicorn server:app --host 0.0.0.0 --port $PORT"
